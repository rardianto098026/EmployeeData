@model EmployeeData.Models.EmployeeDataModels
<div>
<div class="dataTables_wrapper form-inline no-footer">
    <div class="ui-jqgrid-hdiv ui-state-default">
        <div class="ui-jqgrid-hbox">
            <table id="dynamic-table" class="table table-striped table-bordered table-hover dataTable no-footer" role="grid" aria-describedby="dynamic-table_info">
                <thead>
                    <tr role="row" style="font-size:12px">
                        <th tabindex="0" aria-controls="dynamic-table" rowspan="1" colspan="1">No</th>
                        <th tabindex="2" aria-controls="dynamic-table" rowspan="1" colspan="1">Name</th>
                        <th tabindex="3" aria-controls="dynamic-table" rowspan="1" colspan="1">Birthdate</th>
                        <th tabindex="5" aria-controls="dynamic-table" rowspan="1" colspan="1">Gender</th>
                        <th tabindex="6" aria-controls="dynamic-table" rowspan="1" colspan="1">NIK</th>
                        <th tabindex="6" aria-controls="dynamic-table" rowspan="1" colspan="1">idx</th>
                        <th rowspan="1" colspan="1" aria-label=""></th>
                    </tr>
                </thead>

                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr style="font-size:12px">
                            <td>
                                @Html.DisplayFor(m => item.No)
                            </td>
                            <td>
                                @Html.DisplayFor(m => item.Name)
                            </td>
                            <td>
                                @Html.DisplayFor(m => item.Birthdate)
                            </td>

                            <td>
                                @Html.DisplayFor(m => item.Gender)
                            </td>
                            <td>
                                @Html.DisplayFor(m => item.NIK)
                            </td>
                            <td>
                                @Html.DisplayFor(m => item.idx)
                            </td>
                            <td>
                                <div class="hidden-sm hidden-xs action-buttons">
                                    <a class="green" href="@Url.Action("Edit", "UserMatrix", new { NIK = item.NIK, Name = item.Name, Employee = item.Entity })">
                                        Edit
                                    </a>

                                    <a class="red" href="#">
                                        @Html.ActionLink("Delete", "DeleteUser", new { id = item.NIK }, new { @onclick = "return confirm('Are you sure you want to delete this record?');" })
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
</div>